# Runtime dependencies
# ====================
# This project has no Python runtime dependencies (standard library only).
#
# Development dependencies:
#   pip install -r requirements-dev.txt
#
# System dependencies (LaTeX/ImageMagick/Pandoc) are documented in:
#   .github/SYSTEM_REQUIREMENTS.md
